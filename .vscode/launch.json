{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Redis Clone",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/main",
            "args": [],
            "cwd": "${workspaceFolder}",
            "stopAtEntry": false,
            "externalConsole": false,
            "MIMode": "lldb",
            "preLaunchTask": "build redis clone"
        }
    ]
}